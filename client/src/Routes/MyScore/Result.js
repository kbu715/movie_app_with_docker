import React from 'react';
import Popup from 'reactjs-popup';

const Result = () => {
    return (
            <Popup
        trigger={<button className="button">결과보기</button>}
        modal
        closeOnDocumentClick
      >
          wwewewe
      </Popup>
    );
};

export default Result;